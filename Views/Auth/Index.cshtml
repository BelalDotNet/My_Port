@using My_Port.Dto
@model List<UserDto>;

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Users</h3>

                <div class="card-tools">
                    <div class="input-group input-group-sm" style="width: 150px;">
                        <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0" style="height: 300px;">
                <table class="table table-bordered table-head-fixed text-nowrap">
                    <thead>
                        <tr>
                            <th>SL</th>
                            <th>User Name</th>
                            <th>Email</th>
                            <th>Status</th>
                           
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            if (Model != null && Model.Count > 0)
                            {
                                int sl = 1;
                                foreach (var usr in Model)
                                {
                                    <tr>
                                        <td>@(sl++)</td>
                                        <td>@usr.UserName</td>
                                        <td>@usr.Email</td>
                                        <td>Active</td>
                                       
                                    </tr>
                                }
                            }else
                            {
                                <tr>
                                    <td colspan="4" class="text-center">No users found.</td>
                                </tr>
                            }
                        }
                            </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>

@* <div>
</div>
<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3 class="m-0">User List</h3>
    </div>

    <div class="card shadow p-3">
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Name</th>
                    <th>Department</th>
                    <th>Email</th>
                    <th>Designation</th>
                    <th style="width: 150px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @{
                    if (Model != null && Model.Count > 0)
                    {
                        foreach (var employee in Model)
                        {
                            <tr>
                                <td>@employee.EmployeeName</td>
                                <td>@employee.Department</td>
                                <td>@employee.Email</td>
                                <td>@employee.Designation</td>
                                <td>
                                    <a class="btn btn-sm btn-primary" asp-action="UpdateEmployee" asp-route-id="@employee.Id">Edit</a>
                                    <a class="btn btn-sm btn-danger" asp-action="DeleteEmployee" asp-route-id="@employee.Id" onclick="return confirm('Are you sure you want to delete this role?');">Delete</a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center">No employees found.</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

</div> *@

